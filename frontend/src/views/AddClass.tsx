export const AddClassView = () => {
  /**
   * useCOntext for states defined in ToInsert
   */

  /**
   * useEffect(() => {
   *  if(no word selected for editing in state)
   *    navigate('/ToInsert')
   * }, [])
   */

  return(
    <>Add word class view</>
  )
}