
import '../styles/layout/header.css'
import { IHeaderProps } from '../interfaces/IProps'

export const Header = ({}: IHeaderProps) => {
  return (
    <header className='site__layout__header'> Insert Interface </header>
  )
}
